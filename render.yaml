services:
  - type: web
    name: sapanjai-ai
    env: python
    plan: free  # or whatever plan you use
    buildCommand: python scripts/preload_models.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: HF_TOKEN
        sync: false